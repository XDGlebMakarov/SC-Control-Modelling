params = struct('mu',398600.4416, ... % км^3/c^2, грав параметр
                'i',pi/6, ... % рад, наклонение
                'e',0.1, ... % эксцентриситет
                'p',11000, ... % км, фокальный параметр
                'Omega',pi/4, ... % рад, аргумент восходящего узла
                'omega',pi/3); % рад, аргумент перицентра
[r0, v0] = elem2xyz(params.i,params.e,params.p,params.Omega,params.omega,0,params.mu);
x_START = [r0;  % радиус-вектор, км
           v0]; % скорость, км/с
T = 1e5; % с
dt = 0.5;